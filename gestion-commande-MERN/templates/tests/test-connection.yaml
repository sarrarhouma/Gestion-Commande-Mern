apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gestion-commande-MERN.fullname" . }}-test-connection"
  labels:
    {{- include "gestion-commande-MERN.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gestion-commande-MERN.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
